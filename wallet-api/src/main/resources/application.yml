spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/showcase_db
    username: admin
    password: adminpassword
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
  graphql:
    graphiql:
      enabled: true